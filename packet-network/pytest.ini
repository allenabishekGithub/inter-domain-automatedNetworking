[pytest]
# Modules sit at the package root, tests one level down. Each component is run
# in its own pytest session: both define a top-level main.py, so they must not
# share an interpreter's module namespace.
pythonpath = .
testpaths = tests
addopts = -q
